const socket=io();let S={room:'',selfId:'',players:[],hostId:'',phase:'lobby',round:0,turnIndex:0,timer:0,qa:[],chat:[],votes:{},locations:[]},role={spy:false,location:null,mission:''};const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function screen(id){document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));$(id).classList.add('active')}function msg(t){$('msg').textContent=t||''}function render(){if(S.phase==='lobby'){screen('room');$('roomCode').textContent=S.room;$('count').textContent=`(${S.players.length}/12)`;$('players').innerHTML=S.players.map(p=>`<div class="player"><span>● ${esc(p.name)}</span>${p.id===S.hostId?'<span class="pill">HOST</span>':''}</div>`).join('');$('roomSpy').value=String(S.spyCount||1);$('roomSpy').disabled=S.selfId!==S.hostId;$('start').disabled=S.selfId!==S.hostId||S.players.length<3;$('roomMsg').textContent=S.players.length<3?'Tối thiểu 3 người để bắt đầu.':S.selfId===S.hostId?'Có thể bắt đầu.':'Chờ Host bắt đầu.'}else if(S.phase==='game'||S.phase==='discussion'){screen('game');$('round').textContent=S.round;$('gameCode').textContent=S.room;$('timer').textContent=`${String(Math.floor(S.timer/60)).padStart(2,'0')}:${String(S.timer%60).padStart(2,'0')}`;$('mission').textContent=role.mission||'Đang chia nhiệm vụ...';$('done').disabled=!!role.done;$('done').textContent=role.done?'✓ Đã hoàn thành · +2 điểm':'✓ Tôi đã hoàn thành nhiệm vụ';$('turn').textContent=S.phase==='discussion'?'THẢO LUẬN':S.players[S.turnIndex]?.name||'';const me=S.players[S.turnIndex]?.id===S.selfId;const pending=S.pending;const can=me&&!pending;const targets=S.players.filter(p=>p.id!==S.selfId&&!S.asked?.includes(p.id));$('target').innerHTML=targets.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');$('question').disabled=!can;$('ask').disabled=!can;$('instruction').textContent=S.phase==='discussion'?'Mọi người đã được hỏi. Thảo luận và vote.':pending?(pending.toId===S.selfId?'Bạn cần trả lời câu hỏi.':'Đang chờ câu trả lời...'):(me?'Chọn người và đặt câu hỏi.':`Đang chờ ${S.players[S.turnIndex]?.name||'người chơi'}...`);$('answer').classList.toggle('hidden',!(pending&&pending.toId===S.selfId));if(pending&&pending.toId===S.selfId)$('incoming').textContent=`${esc(S.players.find(p=>p.id===pending.fromId)?.name||'')} hỏi: ${esc(pending.q)}`;$('qa').innerHTML=(S.qa||[]).map(x=>`<div><b>${esc(x.from)} → ${esc(x.to)}</b><br>❓ ${esc(x.q)}<br>💬 ${esc(x.a)}</div>`).join('')||'<span class="muted">Chưa có câu hỏi.</span>';$('chat').innerHTML=(S.chat||[]).map(x=>`<p><b>${esc(x.from||'Hệ thống')}:</b> ${esc(x.message)}</p>`).join('');$('chat').scrollTop=$('chat').scrollHeight;$('scores').innerHTML=S.players.map(p=>`<div class="score"><span>${esc(p.name)}</span><b>${p.score||0}</b></div>`).join('');renderLocations();renderVote()}else if(S.phase==='result')screen('result')}function renderLocations(){const names=S.locations||[];$('locations').innerHTML=names.map((n,i)=>`<div class="loc" data-loc="${esc(n)}"><img src="/assets/${encodeURIComponent(n)}.webp"><span>${i+1}. ${esc(n)}</span></div>`).join('');$('guessLoc').innerHTML=names.map(n=>`<option>${esc(n)}</option>`).join('');document.querySelectorAll('.loc').forEach(el=>el.onclick=()=>{if(role.spy&&confirm(`Đoán địa điểm là “${el.dataset.loc}”?`))socket.emit('guess',{location:el.dataset.loc})})}function renderVote(){const b=$('voteBox');if(S.phase!=='discussion'){b.classList.add('hidden');return}b.classList.remove('hidden');b.innerHTML=S.players.filter(p=>p.id!==S.selfId).map(p=>`<button class="secondary" style="width:100%;margin:4px 0" data-v="${p.id}">${esc(p.name)} ${S.votes?.[p.id]?'✓':''}</button>`).join('');b.querySelectorAll('[data-v]').forEach(x=>x.onclick=()=>socket.emit('vote',{targetId:x.dataset.v}))}socket.on('joined',x=>{S.room=x.room;S.selfId=x.selfId;screen('room')});socket.on('state',x=>{S=x;render()});socket.on('role',x=>{role=x;render()});socket.on('question',x=>{S.pending=x;render()});socket.on('result',r=>{$('resultTitle').textContent=r.title;$('resultDetail').textContent=r.detail;$('resultLocation').textContent=`📍 Địa điểm: ${r.location}`;$('resultScores').innerHTML=r.scores?Object.entries(r.scores).sort((a,b)=>b[1]-a[1]).map(([id,v])=>`<div class="score"><span>${esc(S.players.find(p=>p.id===id)?.name||'')}</span><b>${v}</b></div>`).join(''):'';$('next').style.display=S.hostId===S.selfId?'block':'none';screen('result')});socket.on('mission_ok',()=>{role.done=true;render()});socket.on('error_msg',msg);$('create').onclick=()=>{if(!$('name').value.trim())return msg('Nhập tên trước.');socket.emit('create_room',{name:$('name').value.trim(),spyCount:+$('spyCount').value})};$('join').onclick=()=>{if(!$('name').value.trim())return msg('Nhập tên trước.');socket.emit('join_room',{room:$('code').value.trim(),name:$('name').value.trim()})};$('start').onclick=()=>socket.emit('start');$('roomSpy').onchange=()=>socket.emit('set_spy_count',{count:+$('roomSpy').value});$('leave').onclick=()=>socket.emit('leave');$('ask').onclick=()=>socket.emit('ask',{targetId:$('target').value,q:$('question').value});$('answerBtn').onclick=()=>socket.emit('answer',{answer:$('answerText').value});$('done').onclick=()=>socket.emit('mission_done');$('guess').onclick=()=>{$('guessBox').classList.toggle('hidden')};$('guessBtn').onclick=()=>socket.emit('guess',{location:$('guessLoc').value});$('chatBtn').onclick=()=>{const x=$('chatInput');if(x.value.trim())socket.emit('chat',{message:x.value.trim()});x.value=''};$('next').onclick=()=>socket.emit('next');setInterval(()=>{if(S.timer>0&&['game','discussion'].includes(S.phase)){S.timer--;const t=$('timer');if(t)t.textContent=`${String(Math.floor(S.timer/60)).padStart(2,'0')}:${String(S.timer%60).padStart(2,'0')}`}},1000);